---
import BaseHead from 'component:BaseHead.astro';
import BlogHeader from 'component:BlogHeader.astro';
import BlogPost from 'component:BlogPost.astro';

const {content} = Astro.props;
const {title, description, publishDate, author, heroImage, permalink, alt} = content;
---

<html lang={ content.lang || 'en' }>
  <head>
    <BaseHead title={title} description={description} permalink={permalink} />
    <link rel="stylesheet" href={Astro.resolve('../styles/blog.css')} />   
    <!-- Styles -->
    <link rel="stylesheet" href={Astro.resolve('../styles/xhighlight.css')} />
  </head>

  <body>
    <BlogHeader />
    <div class="wrapper">
      <BlogPost title={title} author={author} heroImage={heroImage} publishDate={publishDate} alt={alt}>
        <slot />
      </BlogPost>
    </div>
  </body>
</html>

